---
"eve": patch
---

The dev TUI now preserves multi-line paste, accepts pasted answers in text questions, and supports `Shift+Enter`. Tall prompts stay bounded while editing preserves Unicode characters.
